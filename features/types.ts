// by establishing a type in TS. The editor immediately knows what methods can be called on that specific type;

const today = new Date();
today.getMonth();

const person = {
  age: 20
};

class Color {}

const red = new Color();

const blue = new Color();
